{
  "name": "@thefoundation/monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "scripts": {
    "compile": "lerna run compile --stream",
    "test": "lerna run test --stream"
  },
  "author": "",
  "license": "MIT",
  "workspaces": {
    "nohoist": [
      "**/@graphprotocol/graph-ts",
      "**/@graphprotocol/graph-ts/**"
    ],
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  } 
}
